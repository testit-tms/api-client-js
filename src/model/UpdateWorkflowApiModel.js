/**
 * API
 * No description provided (generated by Openapi Generator https://github.com/openapitools/openapi-generator)
 *
 * The version of the OpenAPI document: v2.0
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import WorkflowStatusApiModel from './WorkflowStatusApiModel';

/**
 * The UpdateWorkflowApiModel model module.
 * @module model/UpdateWorkflowApiModel
 * @version 7.2.0
 */
class UpdateWorkflowApiModel {
    /**
     * Constructs a new <code>UpdateWorkflowApiModel</code>.
     * @alias module:model/UpdateWorkflowApiModel
     * @param name {String} 
     * @param isDefault {Boolean} 
     * @param statuses {Array.<module:model/WorkflowStatusApiModel>} 
     * @param projectIds {Array.<String>} 
     */
    constructor(name, isDefault, statuses, projectIds) { 
        
        UpdateWorkflowApiModel.initialize(this, name, isDefault, statuses, projectIds);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, name, isDefault, statuses, projectIds) { 
        obj['name'] = name;
        obj['isDefault'] = isDefault;
        obj['statuses'] = statuses;
        obj['projectIds'] = projectIds;
    }

    /**
     * Constructs a <code>UpdateWorkflowApiModel</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/UpdateWorkflowApiModel} obj Optional instance to populate.
     * @return {module:model/UpdateWorkflowApiModel} The populated <code>UpdateWorkflowApiModel</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new UpdateWorkflowApiModel();

            if (data.hasOwnProperty('name')) {
                obj['name'] = ApiClient.convertToType(data['name'], 'String');
            }
            if (data.hasOwnProperty('isDefault')) {
                obj['isDefault'] = ApiClient.convertToType(data['isDefault'], 'Boolean');
            }
            if (data.hasOwnProperty('statuses')) {
                obj['statuses'] = ApiClient.convertToType(data['statuses'], [WorkflowStatusApiModel]);
            }
            if (data.hasOwnProperty('projectIds')) {
                obj['projectIds'] = ApiClient.convertToType(data['projectIds'], ['String']);
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>UpdateWorkflowApiModel</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>UpdateWorkflowApiModel</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of UpdateWorkflowApiModel.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['name'] && !(typeof data['name'] === 'string' || data['name'] instanceof String)) {
            throw new Error("Expected the field `name` to be a primitive type in the JSON string but got " + data['name']);
        }
        if (data['statuses']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['statuses'])) {
                throw new Error("Expected the field `statuses` to be an array in the JSON data but got " + data['statuses']);
            }
            // validate the optional field `statuses` (array)
            for (const item of data['statuses']) {
                WorkflowStatusApiModel.validateJSON(item);
            };
        }
        // ensure the json data is an array
        if (!Array.isArray(data['projectIds'])) {
            throw new Error("Expected the field `projectIds` to be an array in the JSON data but got " + data['projectIds']);
        }

        return true;
    }


}

UpdateWorkflowApiModel.RequiredProperties = ["name", "isDefault", "statuses", "projectIds"];

/**
 * @member {String} name
 */
UpdateWorkflowApiModel.prototype['name'] = undefined;

/**
 * @member {Boolean} isDefault
 */
UpdateWorkflowApiModel.prototype['isDefault'] = undefined;

/**
 * @member {Array.<module:model/WorkflowStatusApiModel>} statuses
 */
UpdateWorkflowApiModel.prototype['statuses'] = undefined;

/**
 * @member {Array.<String>} projectIds
 */
UpdateWorkflowApiModel.prototype['projectIds'] = undefined;






export default UpdateWorkflowApiModel;

