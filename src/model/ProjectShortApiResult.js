/**
 * API
 * No description provided (generated by Openapi Generator https://github.com/openapitools/openapi-generator)
 *
 * The version of the OpenAPI document: v2.0
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import ProjectType from './ProjectType';

/**
 * The ProjectShortApiResult model module.
 * @module model/ProjectShortApiResult
 * @version 7.0.0-rc1
 */
class ProjectShortApiResult {
    /**
     * Constructs a new <code>ProjectShortApiResult</code>.
     * @alias module:model/ProjectShortApiResult
     * @param id {String} Unique ID of project
     * @param isDeleted {Boolean} Indicates whether the project is deleted
     * @param globalId {Number} Global ID of project
     * @param name {String} Name of project
     * @param type {module:model/ProjectType} Type of the project
     * @param isFavorite {Boolean} Indicates if the project is marked as favorite
     */
    constructor(id, isDeleted, globalId, name, type, isFavorite) { 
        
        ProjectShortApiResult.initialize(this, id, isDeleted, globalId, name, type, isFavorite);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, id, isDeleted, globalId, name, type, isFavorite) { 
        obj['id'] = id;
        obj['isDeleted'] = isDeleted;
        obj['globalId'] = globalId;
        obj['name'] = name;
        obj['type'] = type;
        obj['isFavorite'] = isFavorite;
    }

    /**
     * Constructs a <code>ProjectShortApiResult</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/ProjectShortApiResult} obj Optional instance to populate.
     * @return {module:model/ProjectShortApiResult} The populated <code>ProjectShortApiResult</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new ProjectShortApiResult();

            if (data.hasOwnProperty('id')) {
                obj['id'] = ApiClient.convertToType(data['id'], 'String');
            }
            if (data.hasOwnProperty('isDeleted')) {
                obj['isDeleted'] = ApiClient.convertToType(data['isDeleted'], 'Boolean');
            }
            if (data.hasOwnProperty('globalId')) {
                obj['globalId'] = ApiClient.convertToType(data['globalId'], 'Number');
            }
            if (data.hasOwnProperty('name')) {
                obj['name'] = ApiClient.convertToType(data['name'], 'String');
            }
            if (data.hasOwnProperty('type')) {
                obj['type'] = ApiClient.convertToType(data['type'], ProjectType);
            }
            if (data.hasOwnProperty('isFavorite')) {
                obj['isFavorite'] = ApiClient.convertToType(data['isFavorite'], 'Boolean');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>ProjectShortApiResult</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>ProjectShortApiResult</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of ProjectShortApiResult.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['id'] && !(typeof data['id'] === 'string' || data['id'] instanceof String)) {
            throw new Error("Expected the field `id` to be a primitive type in the JSON string but got " + data['id']);
        }
        // ensure the json data is a string
        if (data['name'] && !(typeof data['name'] === 'string' || data['name'] instanceof String)) {
            throw new Error("Expected the field `name` to be a primitive type in the JSON string but got " + data['name']);
        }

        return true;
    }


}

ProjectShortApiResult.RequiredProperties = ["id", "isDeleted", "globalId", "name", "type", "isFavorite"];

/**
 * Unique ID of project
 * @member {String} id
 */
ProjectShortApiResult.prototype['id'] = undefined;

/**
 * Indicates whether the project is deleted
 * @member {Boolean} isDeleted
 */
ProjectShortApiResult.prototype['isDeleted'] = undefined;

/**
 * Global ID of project
 * @member {Number} globalId
 */
ProjectShortApiResult.prototype['globalId'] = undefined;

/**
 * Name of project
 * @member {String} name
 */
ProjectShortApiResult.prototype['name'] = undefined;

/**
 * Type of the project
 * @member {module:model/ProjectType} type
 */
ProjectShortApiResult.prototype['type'] = undefined;

/**
 * Indicates if the project is marked as favorite
 * @member {Boolean} isFavorite
 */
ProjectShortApiResult.prototype['isFavorite'] = undefined;






export default ProjectShortApiResult;

