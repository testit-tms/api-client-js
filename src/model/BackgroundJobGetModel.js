/**
 * API
 * No description provided (generated by Openapi Generator https://github.com/openapitools/openapi-generator)
 *
 * The version of the OpenAPI document: v2.0
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import BackgroundJobAttachmentModel from './BackgroundJobAttachmentModel';
import BackgroundJobState from './BackgroundJobState';
import BackgroundJobType from './BackgroundJobType';

/**
 * The BackgroundJobGetModel model module.
 * @module model/BackgroundJobGetModel
 * @version 7.0.0-rc8
 */
class BackgroundJobGetModel {
    /**
     * Constructs a new <code>BackgroundJobGetModel</code>.
     * @alias module:model/BackgroundJobGetModel
     * @param id {String} 
     * @param jobId {String} 
     * @param jobType {module:model/BackgroundJobType} 
     * @param state {module:model/BackgroundJobState} 
     * @param isDeleted {Boolean} 
     * @param progress {Number} 
     * @param createdDate {Date} 
     * @param attachments {Array.<module:model/BackgroundJobAttachmentModel>} 
     */
    constructor(id, jobId, jobType, state, isDeleted, progress, createdDate, attachments) { 
        
        BackgroundJobGetModel.initialize(this, id, jobId, jobType, state, isDeleted, progress, createdDate, attachments);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, id, jobId, jobType, state, isDeleted, progress, createdDate, attachments) { 
        obj['id'] = id;
        obj['jobId'] = jobId;
        obj['jobType'] = jobType;
        obj['state'] = state;
        obj['isDeleted'] = isDeleted;
        obj['progress'] = progress;
        obj['createdDate'] = createdDate;
        obj['attachments'] = attachments;
    }

    /**
     * Constructs a <code>BackgroundJobGetModel</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/BackgroundJobGetModel} obj Optional instance to populate.
     * @return {module:model/BackgroundJobGetModel} The populated <code>BackgroundJobGetModel</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new BackgroundJobGetModel();

            if (data.hasOwnProperty('id')) {
                obj['id'] = ApiClient.convertToType(data['id'], 'String');
            }
            if (data.hasOwnProperty('jobId')) {
                obj['jobId'] = ApiClient.convertToType(data['jobId'], 'String');
            }
            if (data.hasOwnProperty('jobType')) {
                obj['jobType'] = ApiClient.convertToType(data['jobType'], BackgroundJobType);
            }
            if (data.hasOwnProperty('state')) {
                obj['state'] = ApiClient.convertToType(data['state'], BackgroundJobState);
            }
            if (data.hasOwnProperty('isDeleted')) {
                obj['isDeleted'] = ApiClient.convertToType(data['isDeleted'], 'Boolean');
            }
            if (data.hasOwnProperty('progress')) {
                obj['progress'] = ApiClient.convertToType(data['progress'], 'Number');
            }
            if (data.hasOwnProperty('createdDate')) {
                obj['createdDate'] = ApiClient.convertToType(data['createdDate'], 'Date');
            }
            if (data.hasOwnProperty('startDate')) {
                obj['startDate'] = ApiClient.convertToType(data['startDate'], 'Date');
            }
            if (data.hasOwnProperty('endDate')) {
                obj['endDate'] = ApiClient.convertToType(data['endDate'], 'Date');
            }
            if (data.hasOwnProperty('error')) {
                obj['error'] = ApiClient.convertToType(data['error'], 'String');
            }
            if (data.hasOwnProperty('attachments')) {
                obj['attachments'] = ApiClient.convertToType(data['attachments'], [BackgroundJobAttachmentModel]);
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>BackgroundJobGetModel</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>BackgroundJobGetModel</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of BackgroundJobGetModel.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['id'] && !(typeof data['id'] === 'string' || data['id'] instanceof String)) {
            throw new Error("Expected the field `id` to be a primitive type in the JSON string but got " + data['id']);
        }
        // ensure the json data is a string
        if (data['jobId'] && !(typeof data['jobId'] === 'string' || data['jobId'] instanceof String)) {
            throw new Error("Expected the field `jobId` to be a primitive type in the JSON string but got " + data['jobId']);
        }
        // ensure the json data is a string
        if (data['error'] && !(typeof data['error'] === 'string' || data['error'] instanceof String)) {
            throw new Error("Expected the field `error` to be a primitive type in the JSON string but got " + data['error']);
        }
        if (data['attachments']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['attachments'])) {
                throw new Error("Expected the field `attachments` to be an array in the JSON data but got " + data['attachments']);
            }
            // validate the optional field `attachments` (array)
            for (const item of data['attachments']) {
                BackgroundJobAttachmentModel.validateJSON(item);
            };
        }

        return true;
    }


}

BackgroundJobGetModel.RequiredProperties = ["id", "jobId", "jobType", "state", "isDeleted", "progress", "createdDate", "attachments"];

/**
 * @member {String} id
 */
BackgroundJobGetModel.prototype['id'] = undefined;

/**
 * @member {String} jobId
 */
BackgroundJobGetModel.prototype['jobId'] = undefined;

/**
 * @member {module:model/BackgroundJobType} jobType
 */
BackgroundJobGetModel.prototype['jobType'] = undefined;

/**
 * @member {module:model/BackgroundJobState} state
 */
BackgroundJobGetModel.prototype['state'] = undefined;

/**
 * @member {Boolean} isDeleted
 */
BackgroundJobGetModel.prototype['isDeleted'] = undefined;

/**
 * @member {Number} progress
 */
BackgroundJobGetModel.prototype['progress'] = undefined;

/**
 * @member {Date} createdDate
 */
BackgroundJobGetModel.prototype['createdDate'] = undefined;

/**
 * @member {Date} startDate
 */
BackgroundJobGetModel.prototype['startDate'] = undefined;

/**
 * @member {Date} endDate
 */
BackgroundJobGetModel.prototype['endDate'] = undefined;

/**
 * @member {String} error
 */
BackgroundJobGetModel.prototype['error'] = undefined;

/**
 * @member {Array.<module:model/BackgroundJobAttachmentModel>} attachments
 */
BackgroundJobGetModel.prototype['attachments'] = undefined;






export default BackgroundJobGetModel;

