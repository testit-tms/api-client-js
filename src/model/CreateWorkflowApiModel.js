/**
 * API
 * No description provided (generated by Openapi Generator https://github.com/openapitools/openapi-generator)
 *
 * The version of the OpenAPI document: v2.0
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import WorkflowStatusApiModel from './WorkflowStatusApiModel';

/**
 * The CreateWorkflowApiModel model module.
 * @module model/CreateWorkflowApiModel
 * @version 7.2.0
 */
class CreateWorkflowApiModel {
    /**
     * Constructs a new <code>CreateWorkflowApiModel</code>.
     * @alias module:model/CreateWorkflowApiModel
     * @param name {String} 
     * @param statuses {Array.<module:model/WorkflowStatusApiModel>} 
     */
    constructor(name, statuses) { 
        
        CreateWorkflowApiModel.initialize(this, name, statuses);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, name, statuses) { 
        obj['name'] = name;
        obj['statuses'] = statuses;
    }

    /**
     * Constructs a <code>CreateWorkflowApiModel</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/CreateWorkflowApiModel} obj Optional instance to populate.
     * @return {module:model/CreateWorkflowApiModel} The populated <code>CreateWorkflowApiModel</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new CreateWorkflowApiModel();

            if (data.hasOwnProperty('name')) {
                obj['name'] = ApiClient.convertToType(data['name'], 'String');
            }
            if (data.hasOwnProperty('isDefault')) {
                obj['isDefault'] = ApiClient.convertToType(data['isDefault'], 'Boolean');
            }
            if (data.hasOwnProperty('statuses')) {
                obj['statuses'] = ApiClient.convertToType(data['statuses'], [WorkflowStatusApiModel]);
            }
            if (data.hasOwnProperty('projectIds')) {
                obj['projectIds'] = ApiClient.convertToType(data['projectIds'], ['String']);
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>CreateWorkflowApiModel</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>CreateWorkflowApiModel</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of CreateWorkflowApiModel.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['name'] && !(typeof data['name'] === 'string' || data['name'] instanceof String)) {
            throw new Error("Expected the field `name` to be a primitive type in the JSON string but got " + data['name']);
        }
        if (data['statuses']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['statuses'])) {
                throw new Error("Expected the field `statuses` to be an array in the JSON data but got " + data['statuses']);
            }
            // validate the optional field `statuses` (array)
            for (const item of data['statuses']) {
                WorkflowStatusApiModel.validateJSON(item);
            };
        }
        // ensure the json data is an array
        if (!Array.isArray(data['projectIds'])) {
            throw new Error("Expected the field `projectIds` to be an array in the JSON data but got " + data['projectIds']);
        }

        return true;
    }


}

CreateWorkflowApiModel.RequiredProperties = ["name", "statuses"];

/**
 * @member {String} name
 */
CreateWorkflowApiModel.prototype['name'] = undefined;

/**
 * @member {Boolean} isDefault
 */
CreateWorkflowApiModel.prototype['isDefault'] = undefined;

/**
 * @member {Array.<module:model/WorkflowStatusApiModel>} statuses
 */
CreateWorkflowApiModel.prototype['statuses'] = undefined;

/**
 * @member {Array.<String>} projectIds
 */
CreateWorkflowApiModel.prototype['projectIds'] = undefined;






export default CreateWorkflowApiModel;

