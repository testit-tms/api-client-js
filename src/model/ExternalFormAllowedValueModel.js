/**
 * API
 * No description provided (generated by Openapi Generator https://github.com/openapitools/openapi-generator)
 *
 * The version of the OpenAPI document: v2.0
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';

/**
 * The ExternalFormAllowedValueModel model module.
 * @module model/ExternalFormAllowedValueModel
 * @version 7.1.0
 */
class ExternalFormAllowedValueModel {
    /**
     * Constructs a new <code>ExternalFormAllowedValueModel</code>.
     * @alias module:model/ExternalFormAllowedValueModel
     * @param hasChildren {Boolean} 
     */
    constructor(hasChildren) { 
        
        ExternalFormAllowedValueModel.initialize(this, hasChildren);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, hasChildren) { 
        obj['hasChildren'] = hasChildren;
    }

    /**
     * Constructs a <code>ExternalFormAllowedValueModel</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/ExternalFormAllowedValueModel} obj Optional instance to populate.
     * @return {module:model/ExternalFormAllowedValueModel} The populated <code>ExternalFormAllowedValueModel</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new ExternalFormAllowedValueModel();

            if (data.hasOwnProperty('id')) {
                obj['id'] = ApiClient.convertToType(data['id'], 'String');
            }
            if (data.hasOwnProperty('name')) {
                obj['name'] = ApiClient.convertToType(data['name'], 'String');
            }
            if (data.hasOwnProperty('parentId')) {
                obj['parentId'] = ApiClient.convertToType(data['parentId'], 'String');
            }
            if (data.hasOwnProperty('hasChildren')) {
                obj['hasChildren'] = ApiClient.convertToType(data['hasChildren'], 'Boolean');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>ExternalFormAllowedValueModel</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>ExternalFormAllowedValueModel</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of ExternalFormAllowedValueModel.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['id'] && !(typeof data['id'] === 'string' || data['id'] instanceof String)) {
            throw new Error("Expected the field `id` to be a primitive type in the JSON string but got " + data['id']);
        }
        // ensure the json data is a string
        if (data['name'] && !(typeof data['name'] === 'string' || data['name'] instanceof String)) {
            throw new Error("Expected the field `name` to be a primitive type in the JSON string but got " + data['name']);
        }
        // ensure the json data is a string
        if (data['parentId'] && !(typeof data['parentId'] === 'string' || data['parentId'] instanceof String)) {
            throw new Error("Expected the field `parentId` to be a primitive type in the JSON string but got " + data['parentId']);
        }

        return true;
    }


}

ExternalFormAllowedValueModel.RequiredProperties = ["hasChildren"];

/**
 * @member {String} id
 */
ExternalFormAllowedValueModel.prototype['id'] = undefined;

/**
 * @member {String} name
 */
ExternalFormAllowedValueModel.prototype['name'] = undefined;

/**
 * @member {String} parentId
 */
ExternalFormAllowedValueModel.prototype['parentId'] = undefined;

/**
 * @member {Boolean} hasChildren
 */
ExternalFormAllowedValueModel.prototype['hasChildren'] = undefined;






export default ExternalFormAllowedValueModel;

