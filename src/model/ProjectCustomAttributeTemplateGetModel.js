/**
 * API
 * No description provided (generated by Openapi Generator https://github.com/openapitools/openapi-generator)
 *
 * The version of the OpenAPI document: v2.0
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import CustomAttributeModel from './CustomAttributeModel';

/**
 * The ProjectCustomAttributeTemplateGetModel model module.
 * @module model/ProjectCustomAttributeTemplateGetModel
 * @version 7.1.0
 */
class ProjectCustomAttributeTemplateGetModel {
    /**
     * Constructs a new <code>ProjectCustomAttributeTemplateGetModel</code>.
     * @alias module:model/ProjectCustomAttributeTemplateGetModel
     * @param id {String} Unique ID of the custom attributes template
     * @param isDeleted {Boolean} Indicates if the custom attribute template is deleted
     * @param name {String} Name of the custom attribute template
     * @param customAttributeModels {Array.<module:model/CustomAttributeModel>} Attributes of the template
     */
    constructor(id, isDeleted, name, customAttributeModels) { 
        
        ProjectCustomAttributeTemplateGetModel.initialize(this, id, isDeleted, name, customAttributeModels);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, id, isDeleted, name, customAttributeModels) { 
        obj['id'] = id;
        obj['isDeleted'] = isDeleted;
        obj['name'] = name;
        obj['customAttributeModels'] = customAttributeModels;
    }

    /**
     * Constructs a <code>ProjectCustomAttributeTemplateGetModel</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/ProjectCustomAttributeTemplateGetModel} obj Optional instance to populate.
     * @return {module:model/ProjectCustomAttributeTemplateGetModel} The populated <code>ProjectCustomAttributeTemplateGetModel</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new ProjectCustomAttributeTemplateGetModel();

            if (data.hasOwnProperty('id')) {
                obj['id'] = ApiClient.convertToType(data['id'], 'String');
            }
            if (data.hasOwnProperty('isDeleted')) {
                obj['isDeleted'] = ApiClient.convertToType(data['isDeleted'], 'Boolean');
            }
            if (data.hasOwnProperty('name')) {
                obj['name'] = ApiClient.convertToType(data['name'], 'String');
            }
            if (data.hasOwnProperty('customAttributeModels')) {
                obj['customAttributeModels'] = ApiClient.convertToType(data['customAttributeModels'], [CustomAttributeModel]);
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>ProjectCustomAttributeTemplateGetModel</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>ProjectCustomAttributeTemplateGetModel</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of ProjectCustomAttributeTemplateGetModel.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['id'] && !(typeof data['id'] === 'string' || data['id'] instanceof String)) {
            throw new Error("Expected the field `id` to be a primitive type in the JSON string but got " + data['id']);
        }
        // ensure the json data is a string
        if (data['name'] && !(typeof data['name'] === 'string' || data['name'] instanceof String)) {
            throw new Error("Expected the field `name` to be a primitive type in the JSON string but got " + data['name']);
        }
        if (data['customAttributeModels']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['customAttributeModels'])) {
                throw new Error("Expected the field `customAttributeModels` to be an array in the JSON data but got " + data['customAttributeModels']);
            }
            // validate the optional field `customAttributeModels` (array)
            for (const item of data['customAttributeModels']) {
                CustomAttributeModel.validateJSON(item);
            };
        }

        return true;
    }


}

ProjectCustomAttributeTemplateGetModel.RequiredProperties = ["id", "isDeleted", "name", "customAttributeModels"];

/**
 * Unique ID of the custom attributes template
 * @member {String} id
 */
ProjectCustomAttributeTemplateGetModel.prototype['id'] = undefined;

/**
 * Indicates if the custom attribute template is deleted
 * @member {Boolean} isDeleted
 */
ProjectCustomAttributeTemplateGetModel.prototype['isDeleted'] = undefined;

/**
 * Name of the custom attribute template
 * @member {String} name
 */
ProjectCustomAttributeTemplateGetModel.prototype['name'] = undefined;

/**
 * Attributes of the template
 * @member {Array.<module:model/CustomAttributeModel>} customAttributeModels
 */
ProjectCustomAttributeTemplateGetModel.prototype['customAttributeModels'] = undefined;






export default ProjectCustomAttributeTemplateGetModel;

