/**
 * API
 * No description provided (generated by Openapi Generator https://github.com/openapitools/openapi-generator)
 *
 * The version of the OpenAPI document: v2.0
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import TestRunGroupByFailureClassApiResult from './TestRunGroupByFailureClassApiResult';
import TestRunGroupByStatusApiResult from './TestRunGroupByStatusApiResult';
import TestRunGroupByStatusTypeApiResult from './TestRunGroupByStatusTypeApiResult';

/**
 * The TestRunAnalyticApiResult model module.
 * @module model/TestRunAnalyticApiResult
 * @version 7.2.0
 */
class TestRunAnalyticApiResult {
    /**
     * Constructs a new <code>TestRunAnalyticApiResult</code>.
     * @alias module:model/TestRunAnalyticApiResult
     * @param countGroupByStatus {Array.<module:model/TestRunGroupByStatusApiResult>} 
     * @param countGroupByStatusType {Array.<module:model/TestRunGroupByStatusTypeApiResult>} 
     * @param countGroupByFailureClass {Array.<module:model/TestRunGroupByFailureClassApiResult>} 
     */
    constructor(countGroupByStatus, countGroupByStatusType, countGroupByFailureClass) { 
        
        TestRunAnalyticApiResult.initialize(this, countGroupByStatus, countGroupByStatusType, countGroupByFailureClass);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, countGroupByStatus, countGroupByStatusType, countGroupByFailureClass) { 
        obj['countGroupByStatus'] = countGroupByStatus;
        obj['countGroupByStatusType'] = countGroupByStatusType;
        obj['countGroupByFailureClass'] = countGroupByFailureClass;
    }

    /**
     * Constructs a <code>TestRunAnalyticApiResult</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/TestRunAnalyticApiResult} obj Optional instance to populate.
     * @return {module:model/TestRunAnalyticApiResult} The populated <code>TestRunAnalyticApiResult</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new TestRunAnalyticApiResult();

            if (data.hasOwnProperty('countGroupByStatus')) {
                obj['countGroupByStatus'] = ApiClient.convertToType(data['countGroupByStatus'], [TestRunGroupByStatusApiResult]);
            }
            if (data.hasOwnProperty('countGroupByStatusType')) {
                obj['countGroupByStatusType'] = ApiClient.convertToType(data['countGroupByStatusType'], [TestRunGroupByStatusTypeApiResult]);
            }
            if (data.hasOwnProperty('countGroupByFailureClass')) {
                obj['countGroupByFailureClass'] = ApiClient.convertToType(data['countGroupByFailureClass'], [TestRunGroupByFailureClassApiResult]);
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>TestRunAnalyticApiResult</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>TestRunAnalyticApiResult</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of TestRunAnalyticApiResult.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        if (data['countGroupByStatus']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['countGroupByStatus'])) {
                throw new Error("Expected the field `countGroupByStatus` to be an array in the JSON data but got " + data['countGroupByStatus']);
            }
            // validate the optional field `countGroupByStatus` (array)
            for (const item of data['countGroupByStatus']) {
                TestRunGroupByStatusApiResult.validateJSON(item);
            };
        }
        if (data['countGroupByStatusType']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['countGroupByStatusType'])) {
                throw new Error("Expected the field `countGroupByStatusType` to be an array in the JSON data but got " + data['countGroupByStatusType']);
            }
            // validate the optional field `countGroupByStatusType` (array)
            for (const item of data['countGroupByStatusType']) {
                TestRunGroupByStatusTypeApiResult.validateJSON(item);
            };
        }
        if (data['countGroupByFailureClass']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['countGroupByFailureClass'])) {
                throw new Error("Expected the field `countGroupByFailureClass` to be an array in the JSON data but got " + data['countGroupByFailureClass']);
            }
            // validate the optional field `countGroupByFailureClass` (array)
            for (const item of data['countGroupByFailureClass']) {
                TestRunGroupByFailureClassApiResult.validateJSON(item);
            };
        }

        return true;
    }


}

TestRunAnalyticApiResult.RequiredProperties = ["countGroupByStatus", "countGroupByStatusType", "countGroupByFailureClass"];

/**
 * @member {Array.<module:model/TestRunGroupByStatusApiResult>} countGroupByStatus
 */
TestRunAnalyticApiResult.prototype['countGroupByStatus'] = undefined;

/**
 * @member {Array.<module:model/TestRunGroupByStatusTypeApiResult>} countGroupByStatusType
 */
TestRunAnalyticApiResult.prototype['countGroupByStatusType'] = undefined;

/**
 * @member {Array.<module:model/TestRunGroupByFailureClassApiResult>} countGroupByFailureClass
 */
TestRunAnalyticApiResult.prototype['countGroupByFailureClass'] = undefined;






export default TestRunAnalyticApiResult;

