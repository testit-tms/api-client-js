/**
 * API
 * No description provided (generated by Openapi Generator https://github.com/openapitools/openapi-generator)
 *
 * The version of the OpenAPI document: v2.0
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import GlobalSearchItemResult from './GlobalSearchItemResult';

/**
 * The GlobalSearchResponse model module.
 * @module model/GlobalSearchResponse
 * @version 7.2.0
 */
class GlobalSearchResponse {
    /**
     * Constructs a new <code>GlobalSearchResponse</code>.
     * @alias module:model/GlobalSearchResponse
     * @param results {Array.<module:model/GlobalSearchItemResult>} 
     * @param moreResultsAvailable {Boolean} 
     * @param availableResourceTypes {Array.<String>} 
     */
    constructor(results, moreResultsAvailable, availableResourceTypes) { 
        
        GlobalSearchResponse.initialize(this, results, moreResultsAvailable, availableResourceTypes);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, results, moreResultsAvailable, availableResourceTypes) { 
        obj['results'] = results;
        obj['moreResultsAvailable'] = moreResultsAvailable;
        obj['availableResourceTypes'] = availableResourceTypes;
    }

    /**
     * Constructs a <code>GlobalSearchResponse</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/GlobalSearchResponse} obj Optional instance to populate.
     * @return {module:model/GlobalSearchResponse} The populated <code>GlobalSearchResponse</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new GlobalSearchResponse();

            if (data.hasOwnProperty('results')) {
                obj['results'] = ApiClient.convertToType(data['results'], [GlobalSearchItemResult]);
            }
            if (data.hasOwnProperty('moreResultsAvailable')) {
                obj['moreResultsAvailable'] = ApiClient.convertToType(data['moreResultsAvailable'], 'Boolean');
            }
            if (data.hasOwnProperty('availableResourceTypes')) {
                obj['availableResourceTypes'] = ApiClient.convertToType(data['availableResourceTypes'], ['String']);
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>GlobalSearchResponse</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>GlobalSearchResponse</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of GlobalSearchResponse.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        if (data['results']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['results'])) {
                throw new Error("Expected the field `results` to be an array in the JSON data but got " + data['results']);
            }
            // validate the optional field `results` (array)
            for (const item of data['results']) {
                GlobalSearchItemResult.validateJSON(item);
            };
        }
        // ensure the json data is an array
        if (!Array.isArray(data['availableResourceTypes'])) {
            throw new Error("Expected the field `availableResourceTypes` to be an array in the JSON data but got " + data['availableResourceTypes']);
        }

        return true;
    }


}

GlobalSearchResponse.RequiredProperties = ["results", "moreResultsAvailable", "availableResourceTypes"];

/**
 * @member {Array.<module:model/GlobalSearchItemResult>} results
 */
GlobalSearchResponse.prototype['results'] = undefined;

/**
 * @member {Boolean} moreResultsAvailable
 */
GlobalSearchResponse.prototype['moreResultsAvailable'] = undefined;

/**
 * @member {Array.<String>} availableResourceTypes
 */
GlobalSearchResponse.prototype['availableResourceTypes'] = undefined;






export default GlobalSearchResponse;

