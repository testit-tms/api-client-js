/**
 * API
 * No description provided (generated by Openapi Generator https://github.com/openapitools/openapi-generator)
 *
 * The version of the OpenAPI document: v2.0
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import WorkItemExternalMetadataFieldFilterModel from './WorkItemExternalMetadataFieldFilterModel';

/**
 * The WorkItemExternalMetadataFilterModel model module.
 * @module model/WorkItemExternalMetadataFilterModel
 * @version 7.0.0
 */
class WorkItemExternalMetadataFilterModel {
    /**
     * Constructs a new <code>WorkItemExternalMetadataFilterModel</code>.
     * @alias module:model/WorkItemExternalMetadataFilterModel
     */
    constructor() { 
        
        WorkItemExternalMetadataFilterModel.initialize(this);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj) { 
    }

    /**
     * Constructs a <code>WorkItemExternalMetadataFilterModel</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/WorkItemExternalMetadataFilterModel} obj Optional instance to populate.
     * @return {module:model/WorkItemExternalMetadataFilterModel} The populated <code>WorkItemExternalMetadataFilterModel</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new WorkItemExternalMetadataFilterModel();

            if (data.hasOwnProperty('ids')) {
                obj['ids'] = ApiClient.convertToType(data['ids'], [WorkItemExternalMetadataFieldFilterModel]);
            }
            if (data.hasOwnProperty('types')) {
                obj['types'] = ApiClient.convertToType(data['types'], [WorkItemExternalMetadataFieldFilterModel]);
            }
            if (data.hasOwnProperty('priorities')) {
                obj['priorities'] = ApiClient.convertToType(data['priorities'], [WorkItemExternalMetadataFieldFilterModel]);
            }
            if (data.hasOwnProperty('statuses')) {
                obj['statuses'] = ApiClient.convertToType(data['statuses'], [WorkItemExternalMetadataFieldFilterModel]);
            }
            if (data.hasOwnProperty('assignees')) {
                obj['assignees'] = ApiClient.convertToType(data['assignees'], [WorkItemExternalMetadataFieldFilterModel]);
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>WorkItemExternalMetadataFilterModel</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>WorkItemExternalMetadataFilterModel</code>.
     */
    static validateJSON(data) {
        if (data['ids']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['ids'])) {
                throw new Error("Expected the field `ids` to be an array in the JSON data but got " + data['ids']);
            }
            // validate the optional field `ids` (array)
            for (const item of data['ids']) {
                WorkItemExternalMetadataFieldFilterModel.validateJSON(item);
            };
        }
        if (data['types']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['types'])) {
                throw new Error("Expected the field `types` to be an array in the JSON data but got " + data['types']);
            }
            // validate the optional field `types` (array)
            for (const item of data['types']) {
                WorkItemExternalMetadataFieldFilterModel.validateJSON(item);
            };
        }
        if (data['priorities']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['priorities'])) {
                throw new Error("Expected the field `priorities` to be an array in the JSON data but got " + data['priorities']);
            }
            // validate the optional field `priorities` (array)
            for (const item of data['priorities']) {
                WorkItemExternalMetadataFieldFilterModel.validateJSON(item);
            };
        }
        if (data['statuses']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['statuses'])) {
                throw new Error("Expected the field `statuses` to be an array in the JSON data but got " + data['statuses']);
            }
            // validate the optional field `statuses` (array)
            for (const item of data['statuses']) {
                WorkItemExternalMetadataFieldFilterModel.validateJSON(item);
            };
        }
        if (data['assignees']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['assignees'])) {
                throw new Error("Expected the field `assignees` to be an array in the JSON data but got " + data['assignees']);
            }
            // validate the optional field `assignees` (array)
            for (const item of data['assignees']) {
                WorkItemExternalMetadataFieldFilterModel.validateJSON(item);
            };
        }

        return true;
    }


}



/**
 * Identifiers of external issues to search for
 * @member {Array.<module:model/WorkItemExternalMetadataFieldFilterModel>} ids
 */
WorkItemExternalMetadataFilterModel.prototype['ids'] = undefined;

/**
 * Types of external issues to search for
 * @member {Array.<module:model/WorkItemExternalMetadataFieldFilterModel>} types
 */
WorkItemExternalMetadataFilterModel.prototype['types'] = undefined;

/**
 * Priorities of external issues to search for
 * @member {Array.<module:model/WorkItemExternalMetadataFieldFilterModel>} priorities
 */
WorkItemExternalMetadataFilterModel.prototype['priorities'] = undefined;

/**
 * Statuses of external issues to search for
 * @member {Array.<module:model/WorkItemExternalMetadataFieldFilterModel>} statuses
 */
WorkItemExternalMetadataFilterModel.prototype['statuses'] = undefined;

/**
 * Assignees of external issues to search for
 * @member {Array.<module:model/WorkItemExternalMetadataFieldFilterModel>} assignees
 */
WorkItemExternalMetadataFilterModel.prototype['assignees'] = undefined;






export default WorkItemExternalMetadataFilterModel;

