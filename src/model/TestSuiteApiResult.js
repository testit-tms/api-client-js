/**
 * API
 * No description provided (generated by Openapi Generator https://github.com/openapitools/openapi-generator)
 *
 * The version of the OpenAPI document: v2.0
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import TestSuiteTypeApiResult from './TestSuiteTypeApiResult';

/**
 * The TestSuiteApiResult model module.
 * @module model/TestSuiteApiResult
 * @version 7.0.0
 */
class TestSuiteApiResult {
    /**
     * Constructs a new <code>TestSuiteApiResult</code>.
     * @alias module:model/TestSuiteApiResult
     * @param id {String} Unique ID of the test suite
     * @param name {String} Name of the test suite
     * @param type {module:model/TestSuiteTypeApiResult} Type of the test suite
     * @param saveStructure {Boolean} Flag indicating whether the structure of the test suite should be saved
     * @param autoRefresh {Boolean} Flag indicating whether auto-refresh functionality is enabled for the test suite
     * @param refreshDate {Date} The last time the test suite's results were refreshed
     * @param parentId {String} Unique ID of the parent test suite, if any
     * @param testPlanId {String} Unique ID of the associated test plan
     */
    constructor(id, name, type, saveStructure, autoRefresh, refreshDate, parentId, testPlanId) { 
        
        TestSuiteApiResult.initialize(this, id, name, type, saveStructure, autoRefresh, refreshDate, parentId, testPlanId);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, id, name, type, saveStructure, autoRefresh, refreshDate, parentId, testPlanId) { 
        obj['id'] = id;
        obj['name'] = name;
        obj['type'] = type;
        obj['saveStructure'] = saveStructure;
        obj['autoRefresh'] = autoRefresh;
        obj['refreshDate'] = refreshDate;
        obj['parentId'] = parentId;
        obj['testPlanId'] = testPlanId;
    }

    /**
     * Constructs a <code>TestSuiteApiResult</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/TestSuiteApiResult} obj Optional instance to populate.
     * @return {module:model/TestSuiteApiResult} The populated <code>TestSuiteApiResult</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new TestSuiteApiResult();

            if (data.hasOwnProperty('id')) {
                obj['id'] = ApiClient.convertToType(data['id'], 'String');
            }
            if (data.hasOwnProperty('name')) {
                obj['name'] = ApiClient.convertToType(data['name'], 'String');
            }
            if (data.hasOwnProperty('type')) {
                obj['type'] = ApiClient.convertToType(data['type'], TestSuiteTypeApiResult);
            }
            if (data.hasOwnProperty('saveStructure')) {
                obj['saveStructure'] = ApiClient.convertToType(data['saveStructure'], 'Boolean');
            }
            if (data.hasOwnProperty('autoRefresh')) {
                obj['autoRefresh'] = ApiClient.convertToType(data['autoRefresh'], 'Boolean');
            }
            if (data.hasOwnProperty('refreshDate')) {
                obj['refreshDate'] = ApiClient.convertToType(data['refreshDate'], 'Date');
            }
            if (data.hasOwnProperty('parentId')) {
                obj['parentId'] = ApiClient.convertToType(data['parentId'], 'String');
            }
            if (data.hasOwnProperty('testPlanId')) {
                obj['testPlanId'] = ApiClient.convertToType(data['testPlanId'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>TestSuiteApiResult</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>TestSuiteApiResult</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of TestSuiteApiResult.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['id'] && !(typeof data['id'] === 'string' || data['id'] instanceof String)) {
            throw new Error("Expected the field `id` to be a primitive type in the JSON string but got " + data['id']);
        }
        // ensure the json data is a string
        if (data['name'] && !(typeof data['name'] === 'string' || data['name'] instanceof String)) {
            throw new Error("Expected the field `name` to be a primitive type in the JSON string but got " + data['name']);
        }
        // ensure the json data is a string
        if (data['parentId'] && !(typeof data['parentId'] === 'string' || data['parentId'] instanceof String)) {
            throw new Error("Expected the field `parentId` to be a primitive type in the JSON string but got " + data['parentId']);
        }
        // ensure the json data is a string
        if (data['testPlanId'] && !(typeof data['testPlanId'] === 'string' || data['testPlanId'] instanceof String)) {
            throw new Error("Expected the field `testPlanId` to be a primitive type in the JSON string but got " + data['testPlanId']);
        }

        return true;
    }


}

TestSuiteApiResult.RequiredProperties = ["id", "name", "type", "saveStructure", "autoRefresh", "refreshDate", "parentId", "testPlanId"];

/**
 * Unique ID of the test suite
 * @member {String} id
 */
TestSuiteApiResult.prototype['id'] = undefined;

/**
 * Name of the test suite
 * @member {String} name
 */
TestSuiteApiResult.prototype['name'] = undefined;

/**
 * Type of the test suite
 * @member {module:model/TestSuiteTypeApiResult} type
 */
TestSuiteApiResult.prototype['type'] = undefined;

/**
 * Flag indicating whether the structure of the test suite should be saved
 * @member {Boolean} saveStructure
 */
TestSuiteApiResult.prototype['saveStructure'] = undefined;

/**
 * Flag indicating whether auto-refresh functionality is enabled for the test suite
 * @member {Boolean} autoRefresh
 */
TestSuiteApiResult.prototype['autoRefresh'] = undefined;

/**
 * The last time the test suite's results were refreshed
 * @member {Date} refreshDate
 */
TestSuiteApiResult.prototype['refreshDate'] = undefined;

/**
 * Unique ID of the parent test suite, if any
 * @member {String} parentId
 */
TestSuiteApiResult.prototype['parentId'] = undefined;

/**
 * Unique ID of the associated test plan
 * @member {String} testPlanId
 */
TestSuiteApiResult.prototype['testPlanId'] = undefined;






export default TestSuiteApiResult;

