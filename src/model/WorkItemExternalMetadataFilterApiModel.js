/**
 * API
 * No description provided (generated by Openapi Generator https://github.com/openapitools/openapi-generator)
 *
 * The version of the OpenAPI document: v2.0
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import WorkItemExternalMetadataFieldFilterApiModel from './WorkItemExternalMetadataFieldFilterApiModel';

/**
 * The WorkItemExternalMetadataFilterApiModel model module.
 * @module model/WorkItemExternalMetadataFilterApiModel
 * @version 7.0.0
 */
class WorkItemExternalMetadataFilterApiModel {
    /**
     * Constructs a new <code>WorkItemExternalMetadataFilterApiModel</code>.
     * @alias module:model/WorkItemExternalMetadataFilterApiModel
     */
    constructor() { 
        
        WorkItemExternalMetadataFilterApiModel.initialize(this);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj) { 
    }

    /**
     * Constructs a <code>WorkItemExternalMetadataFilterApiModel</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/WorkItemExternalMetadataFilterApiModel} obj Optional instance to populate.
     * @return {module:model/WorkItemExternalMetadataFilterApiModel} The populated <code>WorkItemExternalMetadataFilterApiModel</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new WorkItemExternalMetadataFilterApiModel();

            if (data.hasOwnProperty('ids')) {
                obj['ids'] = ApiClient.convertToType(data['ids'], [WorkItemExternalMetadataFieldFilterApiModel]);
            }
            if (data.hasOwnProperty('types')) {
                obj['types'] = ApiClient.convertToType(data['types'], [WorkItemExternalMetadataFieldFilterApiModel]);
            }
            if (data.hasOwnProperty('priorities')) {
                obj['priorities'] = ApiClient.convertToType(data['priorities'], [WorkItemExternalMetadataFieldFilterApiModel]);
            }
            if (data.hasOwnProperty('statuses')) {
                obj['statuses'] = ApiClient.convertToType(data['statuses'], [WorkItemExternalMetadataFieldFilterApiModel]);
            }
            if (data.hasOwnProperty('assignees')) {
                obj['assignees'] = ApiClient.convertToType(data['assignees'], [WorkItemExternalMetadataFieldFilterApiModel]);
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>WorkItemExternalMetadataFilterApiModel</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>WorkItemExternalMetadataFilterApiModel</code>.
     */
    static validateJSON(data) {
        if (data['ids']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['ids'])) {
                throw new Error("Expected the field `ids` to be an array in the JSON data but got " + data['ids']);
            }
            // validate the optional field `ids` (array)
            for (const item of data['ids']) {
                WorkItemExternalMetadataFieldFilterApiModel.validateJSON(item);
            };
        }
        if (data['types']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['types'])) {
                throw new Error("Expected the field `types` to be an array in the JSON data but got " + data['types']);
            }
            // validate the optional field `types` (array)
            for (const item of data['types']) {
                WorkItemExternalMetadataFieldFilterApiModel.validateJSON(item);
            };
        }
        if (data['priorities']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['priorities'])) {
                throw new Error("Expected the field `priorities` to be an array in the JSON data but got " + data['priorities']);
            }
            // validate the optional field `priorities` (array)
            for (const item of data['priorities']) {
                WorkItemExternalMetadataFieldFilterApiModel.validateJSON(item);
            };
        }
        if (data['statuses']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['statuses'])) {
                throw new Error("Expected the field `statuses` to be an array in the JSON data but got " + data['statuses']);
            }
            // validate the optional field `statuses` (array)
            for (const item of data['statuses']) {
                WorkItemExternalMetadataFieldFilterApiModel.validateJSON(item);
            };
        }
        if (data['assignees']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['assignees'])) {
                throw new Error("Expected the field `assignees` to be an array in the JSON data but got " + data['assignees']);
            }
            // validate the optional field `assignees` (array)
            for (const item of data['assignees']) {
                WorkItemExternalMetadataFieldFilterApiModel.validateJSON(item);
            };
        }

        return true;
    }


}



/**
 * Identifiers of external issues to search for
 * @member {Array.<module:model/WorkItemExternalMetadataFieldFilterApiModel>} ids
 */
WorkItemExternalMetadataFilterApiModel.prototype['ids'] = undefined;

/**
 * Types of external issues to search for
 * @member {Array.<module:model/WorkItemExternalMetadataFieldFilterApiModel>} types
 */
WorkItemExternalMetadataFilterApiModel.prototype['types'] = undefined;

/**
 * Priorities of external issues to search for
 * @member {Array.<module:model/WorkItemExternalMetadataFieldFilterApiModel>} priorities
 */
WorkItemExternalMetadataFilterApiModel.prototype['priorities'] = undefined;

/**
 * Statuses of external issues to search for
 * @member {Array.<module:model/WorkItemExternalMetadataFieldFilterApiModel>} statuses
 */
WorkItemExternalMetadataFilterApiModel.prototype['statuses'] = undefined;

/**
 * Assignees of external issues to search for
 * @member {Array.<module:model/WorkItemExternalMetadataFieldFilterApiModel>} assignees
 */
WorkItemExternalMetadataFilterApiModel.prototype['assignees'] = undefined;






export default WorkItemExternalMetadataFilterApiModel;

