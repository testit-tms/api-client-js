/**
 * API
 * No description provided (generated by Openapi Generator https://github.com/openapitools/openapi-generator)
 *
 * The version of the OpenAPI document: v2.0
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import ExternalIssueApiMetadata from './ExternalIssueApiMetadata';

/**
 * The ExternalIssueApiResult model module.
 * @module model/ExternalIssueApiResult
 * @version 7.0.0-rc8
 */
class ExternalIssueApiResult {
    /**
     * Constructs a new <code>ExternalIssueApiResult</code>.
     * @alias module:model/ExternalIssueApiResult
     * @param id {String} Identiief of external issue
     * @param externalId {String} Identifier of external issue in external service
     * @param url {String} Url of external issue
     * @param metadata {module:model/ExternalIssueApiMetadata} Metadata of external issue from external service
     */
    constructor(id, externalId, url, metadata) { 
        
        ExternalIssueApiResult.initialize(this, id, externalId, url, metadata);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, id, externalId, url, metadata) { 
        obj['id'] = id;
        obj['externalId'] = externalId;
        obj['url'] = url;
        obj['metadata'] = metadata;
    }

    /**
     * Constructs a <code>ExternalIssueApiResult</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/ExternalIssueApiResult} obj Optional instance to populate.
     * @return {module:model/ExternalIssueApiResult} The populated <code>ExternalIssueApiResult</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new ExternalIssueApiResult();

            if (data.hasOwnProperty('id')) {
                obj['id'] = ApiClient.convertToType(data['id'], 'String');
            }
            if (data.hasOwnProperty('externalId')) {
                obj['externalId'] = ApiClient.convertToType(data['externalId'], 'String');
            }
            if (data.hasOwnProperty('url')) {
                obj['url'] = ApiClient.convertToType(data['url'], 'String');
            }
            if (data.hasOwnProperty('metadata')) {
                obj['metadata'] = ApiClient.convertToType(data['metadata'], ExternalIssueApiMetadata);
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>ExternalIssueApiResult</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>ExternalIssueApiResult</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of ExternalIssueApiResult.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['id'] && !(typeof data['id'] === 'string' || data['id'] instanceof String)) {
            throw new Error("Expected the field `id` to be a primitive type in the JSON string but got " + data['id']);
        }
        // ensure the json data is a string
        if (data['externalId'] && !(typeof data['externalId'] === 'string' || data['externalId'] instanceof String)) {
            throw new Error("Expected the field `externalId` to be a primitive type in the JSON string but got " + data['externalId']);
        }
        // ensure the json data is a string
        if (data['url'] && !(typeof data['url'] === 'string' || data['url'] instanceof String)) {
            throw new Error("Expected the field `url` to be a primitive type in the JSON string but got " + data['url']);
        }
        // validate the optional field `metadata`
        if (data['metadata']) { // data not null
          ExternalIssueApiMetadata.validateJSON(data['metadata']);
        }

        return true;
    }


}

ExternalIssueApiResult.RequiredProperties = ["id", "externalId", "url", "metadata"];

/**
 * Identiief of external issue
 * @member {String} id
 */
ExternalIssueApiResult.prototype['id'] = undefined;

/**
 * Identifier of external issue in external service
 * @member {String} externalId
 */
ExternalIssueApiResult.prototype['externalId'] = undefined;

/**
 * Url of external issue
 * @member {String} url
 */
ExternalIssueApiResult.prototype['url'] = undefined;

/**
 * Metadata of external issue from external service
 * @member {module:model/ExternalIssueApiMetadata} metadata
 */
ExternalIssueApiResult.prototype['metadata'] = undefined;






export default ExternalIssueApiResult;

